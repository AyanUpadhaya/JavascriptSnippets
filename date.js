
//ternary operations
const isPlaying = true;

result = isPlaying ? "playing": "not playing";

console.log(result);

//date

const date = new Date();

const today = `Today: ${date.getFullYear()},${date.getMonth()+1},${date.getDate()}`;

console.log(today);